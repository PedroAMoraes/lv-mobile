<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ExListView" parent="Theme.AppCompat.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:background">@color/background</item>
        <item name="android:actionBarTheme">@style/MyActionBar</item>
    </style>

    <style name="Theme.ExListView" parent="Base.Theme.ExListView" />
    <style name="MyActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/appBar</item>
    </style>
</resources>